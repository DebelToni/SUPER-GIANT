tokenizer:
  name: "unused"
  cache_dir: "/proj/giant-data/hf_cache/tokenizer_cache"
  use_custom: true
  custom_path: "/proj/giant-data/GIANT/GIANT-Chat/tokenizers/giant_chat_bpe16k"

paths:
  data_root: "/proj/giant-data"
  hf_cache_root: "/proj/giant-data/hf_cache"

outputs:
  processed_root: "/proj/giant-data/GIANT/GIANT-Chat/data/chat"
  rows_per_shard: 65536
  manifest_filename: "datasets_manifest.json"
  stats_filename: "dataset_stats.json"

scheduling:
  seed: 4212
  write_batch_size: 128
  dry_run_preview_rows: 200

stages:
  chat_sft:
    description: "Assistant-only chat SFT rows from Smol-SmolTalk at 256 tokens"
    output_dir: "chat_sft"
    sequence_length: 256
    min_tokens: 8
    pack_sequences: true
    add_eos: false
    deduplicate: false
    normalization:
      nfkc: true
      collapse_whitespace: true
    sources:
      - type: huggingface
        dataset_name: "HuggingFaceTB/smol-smoltalk"
        split: "train"
        streaming: false
        chat_messages_field: "messages"
        chat_role_field: "role"
        chat_content_field: "content"
        chat_assistant_roles: ["assistant"]
        chat_role_prefix: "<|im_start|>{role}\n"
        chat_turn_suffix: "<|im_end|>\n"
        chat_loss_on_prefix: false
        chat_loss_on_suffix: true
